services:
  - type: web
    name: pitch-ia
    env: go
    buildCommand: go mod download && go build -tags netgo -ldflags '-s -w' -o app .
    startCommand: ./app
    envVars:
      - key: PORT
        sync: false
      - key: OPENAI_API_KEY
        sync: false
    plan: starter

